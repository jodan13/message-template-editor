import styles from './footer.module.css'

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <span className={styles.textMuted}>Developed by Eugene</span>
    </footer>
  )
}
export default Footer